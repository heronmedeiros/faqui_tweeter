<!DOCTYPE html>
<html>
<head>
  <title>FakeTwitter</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="/">
          <%= image_tag("pirate_twitter.png", :alt => "fake", class: "logo") %>
          <span>
            Fake Twitter
          </span>
        </a>
      </div>
      <% if logged_in? %>
        <div class="new_tweet">
          <%= link_to image_tag("new_tweet.png", class: "new_tweet_img"), new_tweet_path %>
        </div>
        <div><%= link_to "Log out", logout_path, method: "delete", class: "login" %></div>
      <% else %>
          <div><%= link_to "Log in", login_path, class: "login" %></div>
      <% end %>
    </div>
  </nav>

  <div id="main-container" class="container">
    <!-- <div class="header clearfix"> -->
    <% flash.each do |name, msg| -%>
      <%= content_tag :div, msg, class: "alert alert-#{name}" %>
    <% end -%>
      <%= yield %>
    <!-- </div> -->
  </div>
</body>
</html>
