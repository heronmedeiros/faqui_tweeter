<% unless current_user.following?(@user) or current_user ==@user%>

  <%= form_for(current_user.on_relationships.build) do |f| %>
    <%= hidden_field_tag :followed_id, @user.id %>
    <%= f.submit "Follow", class: "btn btn-primary" %>
  <% end %>
<% end %>
